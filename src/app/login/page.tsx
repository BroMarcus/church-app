import { login,signup } from './actions'
export default async function LoginPage({searchParams}:{searchParams:Promise<{error?:string;message?:string}>}){const params=await searchParams;return <main className="login-wrap"><div className="login card"><div className="pill">KINGDOM NETWORK • ALPHA</div><h1>Church life, connected.</h1><p className="muted">Community, discipleship, outreach, learning, ministry and schedules in one place.</p>{params.error&&<div className="notice error">{params.error}</div>}{params.message&&<div className="notice success">{params.message}</div>}<form><div className="row"><label className="field" style={{flex:1}}><span>First name</span><input name="first_name" autoComplete="given-name"/></label><label className="field" style={{flex:1}}><span>Last name</span><input name="last_name" autoComplete="family-name"/></label></div><label className="field"><span>Email</span><input name="email" type="email" autoComplete="email" required/></label><label className="field"><span>Password</span><input name="password" type="password" minLength={8} autoComplete="current-password" required/></label><div className="row"><button className="btn" formAction={login}>Sign in</button><button className="btn secondary" formAction={signup}>Create account</button></div></form><p className="small muted" style={{marginTop:16}}>Alpha accounts are automatically connected to New Life Madera while we build the church invitation/onboarding system.</p></div></main>}
